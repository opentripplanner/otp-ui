# Changesets

This directory contains changeset files, which are markdown files that describe the changes
made in a particular branch or PR. They are used to generate changelogs and manage versioning.

For more information about changesets, see https://github.com/changesets/changesets.
