import styled from "styled-components";
import { DotCircle, MapMarkerAlt } from "styled-icons/fa-solid";

const FromIcon = styled(DotCircle)`
  color: #333;
`;

const ToIcon = styled(MapMarkerAlt)`
  color: #f44256;
`;

export { FromIcon, ToIcon };
