import React from "react";

import { ToLocationIcon, FromLocationIcon } from ".";

export default { title: "Common-Icons" };

export const from = () => <FromLocationIcon />;
export const to = () => <ToLocationIcon />;
