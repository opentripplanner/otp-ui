import styled from "styled-components";

const DropdownSelectorWrap = styled.div`
  > div {
    width: 50%;
    display: inline-block;
    padding: 5px;
    box-sizing: border-box;
  }
  select {
    width: 100%;
  }
`;

export default DropdownSelectorWrap;
